<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dcn.spbstu.mysticalcards">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddWordActivity" />
        <activity android:name=".BoxActivity" />
        <activity android:name=".ArchiveActivity" />
        <activity android:name=".DictionarySetActivity" />
        <activity android:name=".Training.ChooseTrainingActivity" />
        <activity
            android:name=".Training.TrainBox"
            android:label="@string/title_activity_train_box1"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".Training.TrainBox_rev_side"
            android:label="TrainBox1_rev_side"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".StorageActivity" />
        <activity android:name=".AddWordVariants" />
        <activity android:name=".AddFromTextActivity"></activity>
    </application>

</manifest>